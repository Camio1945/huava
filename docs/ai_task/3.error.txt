This line is not fully tested of branches:
if (StringUtils.hasText(bearerToken) && bearerToken.startsWith(BEARER_PREFIX))

Every test method's name should start with `should_`, use underscore style.
Use assertj(org.assertj.core.api.Assertions) instead of junit to set assertions.

