[//]: # ()

# Basic Info

- This project is a refactor of docs/source_codes/huava-0.2.4
- The old project uses springboot 3, but the new project uses springboot 4.
- The old project uses java 21, but the new project uses java 25.
- The old project uses hutool 6, but the new project uses hutool 7.
- The new project uses mybatis-plus-boot-starter 3.5.15
- The new project uses lombok 1.18.42
- The new project should have a better design``.
- The new project should have 100% test coverage (except the ones that excluded in pom.xml). Use `mvn clean verify` to check.
- Reference these to achieve a better design and 100% test coverage:
  Markdown papers in docs/source_codes/ZhengYe/doc/md folder
  The docs/source_codes/geektime-todo project
  The docs/source_codes/moco project
- All the todo-list is in docs/todo-list.md, we do it one by one.
- Use English comments.

---

# Current requirements

edit these files, Translate the Chinese comments to English:
src/main/java/cn/huava/common/controller/BaseController.java
src/main/java/cn/huava/common/service/BaseService.java

``Change all java files' comment style to Markdown style, each class's description should be starts with `# `